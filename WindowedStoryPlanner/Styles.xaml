<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <LinearGradientBrush x:Key="RainbowGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#E8F0FE" Offset="0.0"/>
        <GradientStop Color="#FCE8E6" Offset="0.35"/>
        <GradientStop Color="#FEF7E0" Offset="0.65"/>
        <GradientStop Color="#E6F4EA" Offset="1.0"/>
    </LinearGradientBrush>

    <Style x:Key="AddButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="200"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Margin" Value="0,5,5,0"/>
    </Style>
    <Style TargetType="TextBlock" x:Key="LabelStyle">
        <Setter Property="Foreground" Value="#666666"/>
        <Setter Property="Margin" Value="0,0,0,2"/>
        <Setter Property="FontSize" Value="11"/>
    </Style>

    <Style TargetType="TextBox" x:Key="AutoGrowingTextBox">
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="BorderBrush" Value="#CCCCCC"/>
    </Style>

    <Style x:Key="CardBaseStyle" TargetType="UserControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="UserControl">
                    <Border x:Name="CardBorder"
                            BorderBrush="#CCCCCC"
                            BorderThickness="1"
                            CornerRadius="3"
                            Padding="5">

                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick" 
                              Command="{Binding NavigateCommand}"
                              CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

                            <MouseBinding MouseAction="MiddleClick" 
                              Command="{Binding OpenWindowCommand}"/>
                        </Border.InputBindings>

                        <Grid>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                            <TextBlock Text="{Binding ProgressStats}"
                                       VerticalAlignment="Bottom"
                                       HorizontalAlignment="Right"
                                       FontSize="10"
                                       Foreground="#999999"
                                       Margin="0,0,2,-2"/>
                        </Grid>

                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="White"/>

                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasAnalysisPending}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource RainbowGradient}"/>
                                    </DataTrigger>

                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F0F0F0"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="BorderBrush" Value="#999999"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- PlotPointViewModel exposes OpenCommand (opens the common window) but not
     NavigateCommand, OpenWindowCommand, HasAnalysisPending, or ProgressStats.
     Both click gestures open the same window; hover styling is preserved. -->
    <Style x:Key="PlotPointCardStyle" TargetType="UserControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="UserControl">
                    <Border x:Name="CardBorder"
                        BorderBrush="#CCCCCC"
                        BorderThickness="1"
                        CornerRadius="3"
                        Padding="5">

                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick"
                                      Command="{Binding OpenCommand}"/>
                            <MouseBinding MouseAction="MiddleClick"
                                      Command="{Binding OpenCommand}"/>
                        </Border.InputBindings>

                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F0F0F0"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="BorderBrush" Value="#999999"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="EntityCardStyle" TargetType="UserControl" BasedOn="{StaticResource CardBaseStyle}">
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Width" Value="160"/>
        <Setter Property="Height" Value="90"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="SubjectCardStyle" TargetType="UserControl">
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Width" Value="160"/>
        <Setter Property="Height" Value="90"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="UserControl">
                    <Border x:Name="CardBorder"
                            BorderBrush="#CCCCCC"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="8,5"
                            Background="White"
                            Cursor="Hand">

                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding OpenCommand}"/>
                            <MouseBinding MouseAction="MiddleClick"    Command="{Binding OpenCommand}"/>
                        </Border.InputBindings>

                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F0F0F0"/>
                                        <Setter Property="BorderBrush" Value="#999999"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TitleTextBox" TargetType="TextBox" BasedOn="{StaticResource AutoGrowingTextBox}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>
</ResourceDictionary>