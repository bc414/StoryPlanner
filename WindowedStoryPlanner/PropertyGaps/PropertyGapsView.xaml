<UserControl x:Class="WindowedStoryPlanner.PropertyGapsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WindowedStoryPlanner"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=local:PropertyGapsViewModel}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,4">
            <TextBlock DockPanel.Dock="Left" Text="Property Gaps" FontSize="18" FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <TextBlock DockPanel.Dock="Left" Text="{Binding Summary}" Foreground="#555555"
                       VerticalAlignment="Center" Margin="14,0,0,0"/>
            <Button DockPanel.Dock="Right" Content="Refresh" Padding="14,5"
                    Command="{Binding RebuildCommand}"/>
        </DockPanel>

        <TextBlock Grid.Row="1" Margin="0,0,0,10" Foreground="#777777" FontSize="11" TextWrapping="Wrap"
                   Text="Which entities have no value on each property you have defined. Listed in definition order, then by name — nothing here is ranked, and which value an entity should get is yours to decide."/>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding Groups}">
            <ItemsControl.Template>
                <ControlTemplate TargetType="ItemsControl">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsPresenter/>
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Expander Margin="0,0,0,8" IsExpanded="True">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Header}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Scope}" Foreground="#777777" Margin="10,0,0,0"/>
                                <TextBlock Text="gates at:" Foreground="#777777" Margin="14,0,4,0"/>
                                <TextBlock Text="{Binding GatingPhase}" Foreground="#777777"/>
                            </StackPanel>
                        </Expander.Header>
                        <ItemsControl ItemsSource="{Binding Gaps}" Margin="22,4,0,6">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,1">
                                        <Button Content="Open" Padding="8,1" FontSize="11" Margin="0,0,8,0"
                                                Command="{Binding DataContext.OpenOwnerCommand,
                                                          RelativeSource={RelativeSource AncestorType=ItemsControl,
                                                                          AncestorLevel=2}}"
                                                CommandParameter="{Binding}"/>
                                        <TextBlock Text="{Binding OwnerLabel}" VerticalAlignment="Center" FontSize="12"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
