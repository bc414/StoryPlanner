using System.Windows.Controls;

namespace WindowedStoryPlanner;

/// <summary>
/// Interaction logic for CrossCutNoteListView.xaml. DataContext is a
/// <see cref="TaggedNotesViewModelBase"/>; the control has no state of its own.
/// </summary>
public partial class CrossCutNoteListView : UserControl
{
    public CrossCutNoteListView()
    {
        InitializeComponent();
    }
}
