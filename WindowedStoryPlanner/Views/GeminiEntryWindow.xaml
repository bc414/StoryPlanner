<Window x:Class="WindowedStoryPlanner.Views.GeminiEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WindowedStoryPlanner.Views"
        xmlns:models="clr-namespace:StoryPlanner.Models;assembly=StoryPlanner"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=models:GeminiEntry, IsDesignTimeCreatable=True}"
        Title="GeminiEntryWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Time, StringFormat='{}{0:F}'}" 
                       FontSize="20" FontWeight="Bold" Foreground="#555"/>
            <CheckBox Grid.Column="1" IsChecked="{Binding IsAnalyzed}" 
                      Content="Mark as Analyzed" VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Prompt:" FontWeight="Bold" Margin="0,0,0,5"/>
        
            <TextBox Grid.Row="1" Text="{Binding Prompt}" 
                     IsReadOnly="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto"
                     MaxHeight="150" 
                     Padding="10" Background="#F0F0F0" BorderThickness="0"/>
        </Grid>

        <WebBrowser x:Name="ResponseBrowser" Grid.Row="2" />

    </Grid>
</Window>
