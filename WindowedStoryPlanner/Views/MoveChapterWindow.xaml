<Window x:Class="WindowedStoryPlanner.Views.MoveChapterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WindowedStoryPlanner.ViewModels"
        xmlns:v="clr-namespace:WindowedStoryPlanner.Views"
        mc:Ignorable="d"
        Title="Move Chapter" Height="320" Width="420"
        WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance Type=vm:MoveChapterViewModel, IsDesignTimeCreatable=False}">

    <Window.Resources>
        <v:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>

    <DockPanel Margin="14">

        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,10" FontWeight="SemiBold"
                   Text="{Binding Chapter.Title, StringFormat='Move “{0}”'}"
                   TextWrapping="Wrap"/>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Padding="12,4" Margin="0,0,8,0" Click="Cancel_Click" IsCancel="True"/>
            <Button Content="Move" Padding="12,4" Click="Move_Click" IsDefault="True"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Story:" Margin="0,0,0,4"/>
            <ComboBox ItemsSource="{Binding StoryOptions}"
                      SelectedItem="{Binding SelectedStory, Mode=TwoWay}"
                      Margin="0,0,0,14"/>

            <TextBlock Text="Position:" Margin="0,0,0,4"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <RadioButton Content="Before" GroupName="Placement" IsChecked="{Binding PlaceBefore}" Margin="0,0,16,0"/>
                <RadioButton Content="After" GroupName="Placement" IsChecked="{Binding PlaceBefore, Converter={StaticResource InverseBooleanConverter}}"/>
            </StackPanel>

            <TextBlock Text="Relative to (leave unset to place at the end):" Margin="0,0,0,4" TextWrapping="Wrap"/>
            <ComboBox ItemsSource="{Binding AnchorCandidates}"
                      SelectedItem="{Binding SelectedAnchor, Mode=TwoWay}"
                      DisplayMemberPath="FullNumberAndTitle"/>
        </StackPanel>

    </DockPanel>
</Window>
