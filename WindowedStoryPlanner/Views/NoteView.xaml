<UserControl x:Class="WindowedStoryPlanner.Views.NoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowedStoryPlanner.Views"
             xmlns:vm="clr-namespace:WindowedStoryPlanner.ViewModels"
             xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance Type=vm:NoteViewModel}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Border BorderThickness="2"
        CornerRadius="3"
        Padding="5"
        Margin="2"
        BorderBrush="LightGray">
        <DockPanel dd:DragDrop.IsDragSource="True">
            <StackPanel Visibility="{Binding SupportsWorldDate, Converter={StaticResource BoolToVis}}" 
                    Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Right">
                <TextBlock Text="World Date: "/>
                <TextBox Text="{Binding WorldDate}"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="{Binding SortOrder, StringFormat={}{0}.}"/>
            </StackPanel>
            <TextBox Text="{Binding Content}" TextWrapping="Wrap" 
AcceptsReturn="True"
BorderThickness="0"
Background="Transparent"
VerticalAlignment="Stretch"/>
            <StackPanel Visibility="{Binding IsFlagged, Converter={StaticResource BoolToVis}}" Orientation="Horizontal" DockPanel.Dock="Bottom">
                <TextBlock Text="Flag Reason: "/>
                <TextBox Text="{Binding FlagReason}" TextWrapping="Wrap" 
AcceptsReturn="True"
BorderThickness="0"
Background="Transparent"
VerticalAlignment="Stretch"/>
            </StackPanel>
        </DockPanel>
    </Border>
    
</UserControl>
