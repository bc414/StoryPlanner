<UserControl x:Class="WindowedStoryPlanner.Views.NoteViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WindowedStoryPlanner"
             xmlns:v="clr-namespace:WindowedStoryPlanner.Views"
             xmlns:vm="clr-namespace:WindowedStoryPlanner.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:NotesViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="300">

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#F0F0F0" Padding="10,5" BorderBrush="#CCC" BorderThickness="0,0,0,1">
            <DockPanel>
                <TextBlock Text="NOTES" FontWeight="Bold" VerticalAlignment="Center" Foreground="#333"/>
                <Button Content="+" Width="24" Height="24" HorizontalAlignment="Right" 
                        Command="{Binding AddNoteCommand}"/> 
            </DockPanel>
        </Border>

        <ListBox ItemsSource="{Binding TargetCollection}" 
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 BorderThickness="0"
                 Background="Transparent">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <v:NoteCard Margin="0,5" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>