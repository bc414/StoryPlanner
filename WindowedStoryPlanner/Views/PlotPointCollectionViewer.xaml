<UserControl x:Class="WindowedStoryPlanner.Views.PlotPointCollectionViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WindowedStoryPlanner.Views"
             xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
             mc:Ignorable="d" x:Name="RootControl"
             d:DataContext="{x:Static local:Sample.PlotPointCollectionViewModel}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <DataTemplate x:Key="GhostPlotPointTemplate">
            <Border Background="#F0F0F0" 
                BorderBrush="#666666" 
                BorderThickness="2" 
                CornerRadius="3" 
                Padding="10"
                Opacity="0.85" 
                Width="250">
                <StackPanel>
                    <TextBlock Text="Moving Plot Point..." FontWeight="Bold" Foreground="#555" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis" MaxHeight="60"/>
                </StackPanel>

                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="5" Opacity="0.4"/>
                </Border.Effect>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#F0F0F0" Padding="10,5" BorderBrush="#CCC" BorderThickness="0,0,0,1">
            <DockPanel>
                <TextBlock Text="PLOT POINTS" FontWeight="Bold" VerticalAlignment="Center" Foreground="#333"/>
                
            </DockPanel>
        </Border>
        <ListBox x:Name="PlotPointListBox" ItemsSource="{Binding ViewModelCollection}" 
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 BorderThickness="0"
                 Background="Transparent"
                 dd:DragDrop.IsDragSource="True"
                 dd:DragDrop.IsDropTarget="{Binding IsDragReorderEnabled, ElementName=RootControl}"
                 dd:DragDrop.DragAdornerTemplate="{StaticResource GhostPlotPointTemplate}"
                 dd:DragDrop.DragDropContext="Entities">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="dd:DragDrop.IsDropTarget" 
                            Value="{Binding IsDragReorderEnabled, ElementName=RootControl,
                             Converter={StaticResource InverseBooleanConverter}}" />
                    <Setter Property="dd:DragDrop.DropHandler" Value="{Binding}"/>
                    <Setter Property="dd:DragDrop.DragDropContext" Value="Entities" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- The Card -->
                        <local:PlotPointCard />

                        <!-- Reorder Buttons -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0,0,0"
                                    Visibility="{Binding IsDragReorderEnabled, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}">
                            <Button Content="▲" 
                                    Width="20" Height="20" 
                                    Margin="0,0,0,2"
                                    Command="{Binding DataContext.MoveItemUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Move Up"/>
                            <Button Content="▼" 
                                    Width="20" Height="20" 
                                    Command="{Binding DataContext.MoveItemDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Move Down"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>
