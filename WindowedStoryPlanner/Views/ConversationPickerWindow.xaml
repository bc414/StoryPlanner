<Window x:Class="WindowedStoryPlanner.Views.ConversationPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WindowedStoryPlanner.ViewModels"
        mc:Ignorable="d"
        Title="Map to Existing Conversation" Height="520" Width="560"
        WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance Type=vm:ConversationPickerViewModel, IsDesignTimeCreatable=False}">

    <DockPanel Margin="12">

        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,8" TextWrapping="Wrap" FontSize="12" Foreground="#555">
            Pick the existing conversation this export row actually belongs to. Only Claude
            conversations without a stored uuid are listed — the automatic date/block-count guess
            missed this one (or picked wrong), so choose manually below.
        </TextBlock>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Search: " VerticalAlignment="Center"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     MinWidth="260" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Cancel" Padding="12,4" Margin="0,0,8,0" Click="Cancel_Click" IsCancel="True"/>
            <Button Content="Map" Padding="12,4" Click="Map_Click" IsDefault="True"/>
        </StackPanel>

        <ListBox ItemsSource="{Binding FilteredCandidates}"
                 SelectedItem="{Binding SelectedConversation, Mode=TwoWay}"
                 BorderBrush="#CCCCCC" BorderThickness="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4,3">
                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                        <TextBlock FontSize="11" Foreground="#888">
                            <Run Text="{Binding ConversationDate, StringFormat='yyyy-MM-dd', Mode=OneWay}"/>
                            <Run Text=" — "/>
                            <Run Text="{Binding BlockCount, Mode=OneWay}"/> <Run Text=" blocks"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </DockPanel>
</Window>
