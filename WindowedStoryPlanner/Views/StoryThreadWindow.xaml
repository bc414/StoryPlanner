<Window x:Class="WindowedStoryPlanner.Views.StoryThreadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowedStoryPlanner.Views"
        xmlns:vm="clr-namespace:WindowedStoryPlanner.ViewModels"
        xmlns:m="clr-namespace:StoryPlanner.Core.Models;assembly=StoryPlanner.Core"
        xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
        mc:Ignorable="d"
        Title="{Binding Name, StringFormat='Story Thread: {0}'}" 
        Height="550" Width="350"
        d:DataContext="{x:Static local:Sample.StoryThreadViewModel}"
        
        dd:DragDrop.IsDropTarget="{Binding IsLinkingMode}"
        dd:DragDrop.DropHandler="{Binding}">

    <Window.Resources>
        <ObjectDataProvider x:Key="ThreadScopeEnum" MethodName="GetValues" ObjectType="{x:Type m:ThreadScope}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="m:ThreadScope"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="15">
            
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" 
                         Text="{Binding Icon, UpdateSourceTrigger=PropertyChanged}" 
                         FontSize="24" 
                         Width="50" Height="50"
                         HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                         Margin="0,0,10,0"
                         ToolTip="Thread Icon (Emoji)"/>

                <TextBox Grid.Column="1" 
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                         FontSize="18" 
                         FontWeight="Bold" 
                         VerticalContentAlignment="Center"
                         Padding="5"/>
            </Grid>

            <TextBlock Text="SCOPE" Style="{StaticResource LabelStyle}"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource ThreadScopeEnum}}"
                      SelectedItem="{Binding ThreadScope}"/>

            <TextBlock Text="DESCRIPTION" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource AutoGrowingTextBox}"
                     MinHeight="20"/>

            <local:NoteViewer DataContext="{Binding NoteCollectionViewModel}" MinHeight="20" Margin="0,0,0,15"/>

            <local:PlotPointCollectionViewer 
                DataContext="{Binding PlotPointCollectionViewModel}" 
                IsDragReorderEnabled="{Binding IsChecked, ElementName=ReOrderCheckbox}" 
                MinHeight="20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                <Button Content="+ New Plot Point" Height="24" Command="{Binding AddPlotPointCommand}"/>
                <CheckBox x:Name="ReOrderCheckbox" 
                          Content="Reorder Thread Flow" 
                          IsChecked="{Binding IsPlotPointReorderMode}"
                          ToolTip="Enable drag and drop to change the flow of this specific thread"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Window>