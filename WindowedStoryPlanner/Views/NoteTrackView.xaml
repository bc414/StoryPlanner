<UserControl x:Class="WindowedStoryPlanner.Views.NoteTrackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WindowedStoryPlanner.Views"
             xmlns:vm="clr-namespace:WindowedStoryPlanner.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="260"
             d:DataContext="{d:DesignInstance Type=vm:NoteTrackViewModel}">

    <UserControl.Resources>
        <local:CognitiveModeToColorConverter x:Key="CognitiveModeToColor"/>
    </UserControl.Resources>

    <Border BorderThickness="3"
            CornerRadius="6"
            Margin="4"
            BorderBrush="{Binding CognitiveMode, Converter={StaticResource CognitiveModeToColor}}">

        <DockPanel>

            <!-- ── Track header ── -->
            <Border DockPanel.Dock="Top"
                    CornerRadius="3,3,0,0"
                    Padding="8,6"
                    Background="{Binding TrackType,
                                         Converter={StaticResource CognitiveModeToColor}}">

                <Border.ToolTip>
                    <ToolTip MaxWidth="320">
                        <TextBlock Text="{Binding Explanation}"
                                   TextWrapping="Wrap"/>
                    </ToolTip>
                </Border.ToolTip>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Track cognitive mode + name + header text -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CognitiveMode}"
                                   Foreground="#E0FFFFFF"
                                   FontSize="10"
                                   FontStyle="Italic"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding TrackName}"
                                   Foreground="White"
                                   FontWeight="Bold"
                                   FontSize="13"/>
                        <TextBlock Text="{Binding HeaderText}"
                                   Foreground="#E0FFFFFF"
                                   FontSize="11"
                                   FontStyle="Italic"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"
                                   Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- Add-note button — hidden when track is readonly -->
                    <Button Grid.Column="1"
                            Content="+"
                            FontSize="18"
                            FontWeight="Bold"
                            Foreground="White"
                            Background="#33FFFFFF"
                            BorderBrush="#55FFFFFF"
                            BorderThickness="1"
                            Width="28" Height="28"
                            VerticalAlignment="Top"
                            ToolTip="Add new note to this track"
                            Command="{Binding CreateNewNoteCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#55FFFFFF"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#22FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                </Grid>
            </Border>

            <!-- ── Three sections: Confirmed → Unset → Flagged — scrolls vertically per track ── -->
            <ScrollViewer DockPanel.Dock="Bottom"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <!-- RequestBringIntoView is handled here — inside the ScrollViewer —
                     so it is marked handled before bubbling up to ScrollViewer's
                     internal OnRequestBringIntoView override, preventing scroll jumps
                     when programmatic selection changes occur in sibling sections. -->
                <ItemsControl ItemsSource="{Binding Sections}"
                              RequestBringIntoView="SectionsItemsControl_RequestBringIntoView">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:NoteTrackSectionView/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </DockPanel>
    </Border>
</UserControl>
