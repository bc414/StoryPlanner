<Window x:Class="WindowedStoryPlanner.Views.CharacterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:v="clr-namespace:WindowedStoryPlanner.Views"
        xmlns:vm="clr-namespace:WindowedStoryPlanner.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Name, StringFormat='Character: {0}'}" 
        Height="800" Width="500"
        d:DataContext="{x:Static v:Sample.CharacterViewModel}">

    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" Foreground="#555555"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>

        <Style TargetType="TextBox" x:Key="AutoGrowingTextBox">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="15">
        <StackPanel>

            <StackPanel>

                <TextBlock Text="NAME" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
             FontSize="16" FontWeight="Bold" Padding="5" Margin="0,0,0,10"/>

                <TextBlock Text="ARCHETYPE" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Archetype, UpdateSourceTrigger=PropertyChanged}" 
             Padding="5" Margin="0,0,0,10"/>

                <TextBlock Text="INSPIRATION" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Inspiration, UpdateSourceTrigger=PropertyChanged}" 
             Padding="5" Margin="0,0,0,10"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="DESCRIPTION" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
             Style="{StaticResource AutoGrowingTextBox}"
             MinHeight="20"/>
            </StackPanel>

            <v:NoteViewer DataContext="{Binding NoteCollectionViewModel}" MinHeight="200" />

        </StackPanel>
    </ScrollViewer>
</Window>