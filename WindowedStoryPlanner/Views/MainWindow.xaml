<Window x:Class="WindowedStoryPlanner.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WindowedStoryPlanner.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:v="clr-namespace:WindowedStoryPlanner.Views"
        mc:Ignorable="d"
        d:DataContext="{x:Static v:Sample.MainViewModel}"
        Title="Mission Control" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <TabControl TabStripPlacement="Left">
        <TabItem Header="File Management">
            <v:FileManagerTab/>
        </TabItem>
        <TabItem Header="Characters">
            <v:CharacterLibrary /> 
        </TabItem>
        <TabItem Header="Gemini Prompts">
            <v:GeminiLibrary /> 
        </TabItem>

    </TabControl>
    
    
    <!--
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                <TextBlock Text="Story Planner" FontSize="32" FontWeight="Bold" Foreground="#444" 
                           HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <Button Command="{Binding CreateNewProjectCommand}" 
                        Padding="20,15" Margin="0,10" Background="White" BorderBrush="#CCC">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✨" FontSize="20" Margin="0,0,15,0"/>
                        <StackPanel>
                            <TextBlock Text="Create New Project" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Text="Start from scratch with a new database" Foreground="Gray"/>
                        </StackPanel>
                    </StackPanel>
                </Button>

                <Button Command="{Binding OpenProjectCommand}" 
                        Padding="20,15" Margin="0,10" Background="White" BorderBrush="#CCC">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📂" FontSize="20" Margin="0,0,15,0"/>
                        <StackPanel>
                            <TextBlock Text="Open Existing Project" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Text="Load a .db file from your computer" Foreground="Gray"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                
                <Button Command="{Binding SaveChangesCommand}" 
                        Padding="20,15" Margin="0,10" Background="White" BorderBrush="#CCC">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📂" FontSize="20" Margin="0,0,15,0"/>
                        <StackPanel>
                            <TextBlock Text="Save Project" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Text="Save to the .db file" Foreground="Gray"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
            </StackPanel>
            
            <GroupBox Header="Tools" Margin="0,0,0,20" BorderBrush="#CCC">
                <Button Command="{Binding OpenFloatingPointsCommand}" 
                        Padding="15,10" Margin="5" HorizontalAlignment="Left"
                        Background="#FFF0F0" BorderBrush="#E57373">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💡" Margin="0,0,5,0"/>
                        <TextBlock Text="Unplaced Ideas (Floating Points)" FontWeight="Bold"/>
                    </StackPanel>
                </Button>
            </GroupBox>

            <GroupBox Grid.Row="1" Header="Chapters" Margin="0,0,0,20" BorderBrush="#CCC">
                <ItemsControl ItemsSource="{Binding Chapters}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.OpenChapterCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                    CommandParameter="{Binding}"
                                    Margin="5" Padding="10" Width="140" Height="80"
                                    Background="White" BorderBrush="#AAA">
                                <Grid>
                                    <TextBlock Text="{Binding OrderIndex, StringFormat='Ch {0}'}" 
                                               FontSize="24" FontWeight="Bold" Opacity="0.1" 
                                               HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding OrderIndex, StringFormat='Chapter {0}'}" FontSize="10" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>

            <GroupBox Grid.Row="2" Header="Story Threads" Margin="0,0,0,20" BorderBrush="#CCC">
                <ItemsControl ItemsSource="{Binding Threads}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.OpenThreadCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                    CommandParameter="{Binding}"
                                    Margin="5" Padding="10" MinWidth="120"
                                    Background="White">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Fill="{Binding ColorHex}" Margin="0,0,5,0" Stroke="Gray"/>
                                    <TextBlock Text="{Binding Name}" FontWeight="Medium"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>

            <GroupBox Grid.Row="3" Header="Cast" BorderBrush="#CCC">
                <ItemsControl ItemsSource="{Binding Characters}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.OpenCharacterCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                    CommandParameter="{Binding}"
                                    Margin="5" Padding="5" Width="100" Height="120"
                                    Background="White">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Border Background="#EEE" CornerRadius="40" Width="60" Height="60" Margin="0,0,0,5">
                                        <TextBlock Text="{Binding Name}" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                   FontSize="20" FontWeight="Bold" Foreground="#999"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" TextAlignment="Center" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Archetype}" FontSize="10" Foreground="Gray" TextAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>

        </StackPanel>
        
        
    </ScrollViewer>
    -->
</Window>