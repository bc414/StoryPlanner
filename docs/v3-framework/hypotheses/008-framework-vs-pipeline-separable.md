---
id: 8
status: untested
baselined: false
created: 2026-08-31
---

## Hypothesis

Designing the v3 narrative design framework (what the planner tracks) and
designing the v3 operating pipeline (how model, data, instructions, and harness
compose) are related but separable tasks with different evidence bases and
different dependency chains.

## Record

- created | 2026-08-31T20:00: The pipeline hypotheses emerged from a Google
  Keep dump as a cluster distinct from the framework hypotheses (001-005) — the
  two sets ask different questions against different evidence. Framework
  hypotheses test against corpora and the working plan's structure; pipeline
  hypotheses test against lineage evidence, session transcripts, and
  configuration experiments. Brian flagged this separation explicitly: the
  pipeline work (how to compose model/data/instructions/harness) is "related
  but separate" from the framework work (what tracks exist, what the planner
  asks of each subject). Whether this separation holds cleanly — whether
  pipeline choices constrain framework design or vice versa, and whether the
  two need coordinated work units or can proceed independently — is untested.
  The hypothesis is thin: it asserts separability but has no evidence yet for
  where the seams are or whether dependency chains cross the boundary.
