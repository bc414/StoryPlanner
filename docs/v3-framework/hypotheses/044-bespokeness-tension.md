## Hypothesis

V1's codebase was bespoke and vibe-coded, accumulating tech debt, while v2's was clean,
generalizable, and comparatively low in tech debt and bespoke features — and v2's thinness
followed from the weaker tooling (a web-chat interface) it was built with, not from its
cleaner architecture requiring that trade-off. V3's codebase — built with a capability leap
that makes a more complicated codebase easy to produce — has returned toward v1-level
bespokeness and complexity without reintroducing v1-level tech debt.

## Origin

- date: 2026-09-12
- reasoning: Observation: v1's codebase was bespoke, ad hoc ("vibe coded"), and tech-debt-
  laden, while v2's was clean and generalizable but comparatively thin. Brian's assertion,
  typed directly in his Google Keep notes (before 2026-08-30): "V1 had bespokeness but vibe
  coded and tech debt", "V2's thin non bespoke codebase is because I wanted clean code but
  tooling was weaker (web chat)", and "V3 is leap on capability". Raised during a review of
  his own hypotheses about v3, prompted by his sense, in the same notes: "I think I need to
  review the hypotheses again about what v3 and the task ahead is, since some premises
  changed based on evidence". Clarified directly in this session, where he specified what
  "thin" meant: "v2 had a clean code base with minimal tech debt and more generalizable
  features. That's what thin meant. For v3, I have a leap in capability to make a more
  complicated codebase easily, so it can return to v1 levels of bespokeness." He also
  corrected a reading of v3 as a future or hypothetical version: "v3 codebase does exist.
  It's everything starting from Conversation Reader. It's when claude code entered." The
  motivation was to attribute v2's thinness to the tooling it was built with rather than to
  its cleaner architecture — he had wanted clean code and gotten it, so the tooling, not the
  cleanliness, was what fell short — and to locate v3 not as a future prospect but as the
  codebase already produced once that tooling limitation lifted.

## Record
