## unit-047
- section: ## Files and the index
- quote: `docs/v3-framework/hypotheses/NNN-slug.md` — `NNN` a zero-padded three-digit id, stable, …
- counterpart: artifacts.md › Hypothesis index
- relation: narrowed
- note: B keeps the naming rule, the two-column no-status/no-baselined table, and reading status from files, but drops the historical rationale (removed 2026-09-04 as an unchecked hand-kept mirror, the forward-plan-2 parallel) and collapses the three illustrative grep commands (every status, one state, the counts) to the single `sort | uniq -c` one; the hypothesis-file skeleton block is restated in artifacts.md § Hypothesis file.

## unit-048
- section: ## Files and the index
- quote: `status` ∈ `untested | evidenced | challenged`; `baselined` is `false` or an ISO date; …
- counterpart: artifacts.md › Hypothesis file
- relation: narrowed
- note: B keeps the closed field set, the enum, and "no other fields" (with computation rules added); the routing claims — which WUs target a hypothesis lives in the forward plan, connections between hypotheses live in prose — have no counterpart, since forward plans are retired in this revision and hypothesis-to-corpus linkage now lives in a question entry's `hypotheses` line instead.
